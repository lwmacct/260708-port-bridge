# Port Bridge Local

Local-side endpoint for Local Port Bridge. Install it together with
`lwmacct.port-bridge-remote`.

This extension must run in the local UI extension host. It validates the actual
runtime host during activation and stops if it is running remotely.
